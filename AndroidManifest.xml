<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wheat.beautyranking"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity 
            android:name="org.wheat.ranking.activity.LoginActivity"
            android:label="@string/app_name">
            
        </activity>
        <activity 
            android:name="org.wheat.ranking.activity.RegisterActivity"
            android:label="@string/app_name">
            
        </activity>
        <activity
            android:name="org.wheat.ranking.activity.RankingListActivity"
            android:label="@string/app_name"
            android:theme="@style/titleStyle">
            <intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
        </activity>
        <activity 
            android:name="org.wheat.ranking.activity.PhotoCommentActivity">
            
        </activity>
        <activity 
            android:name="org.wheat.ranking.activity.ImageActivityTest"
            android:label="@string/app_name">
            
        </activity>
    </application>

</manifest>
